import React  from 'react';
import '../App.css';

export default function KeyboardHelp() 
{
    return (
      <div>
        <p>
          To unset a letter, press ESC after pressing the letter.
          To invoke UNDO, use the BACKSPACE key.
        </p>
      </div>
    );
}
